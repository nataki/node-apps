{
  "name": "tasks-manager-server",
  "version": "1.0.0",
  "scripts": {
    "start": "tsx --env-file=.env --watch app.ts",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "express": "^5.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.8.0",
    "prisma": "^6.0.0",
    "tsx": "^4.22.0",
    "typescript": "^6.0.3"
  }
}
